name: Build and deploy container app to Azure Web App

on:
  push:
    branches: ["master"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      #  Checkout code
      - uses: actions/checkout@v4

      #  Setup Docker buildx (multi-platform build support)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      #  Login to Azure Container Registry
      - name: Login to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: ${{ secrets.ACR_SERVER }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      #  Build and push Docker image (SHA + latest)
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.ACR_SERVER }}/my-churn-api:${{ github.sha }}
            ${{ secrets.ACR_SERVER }}/my-churn-api:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      #  Login to Azure via Service Principal
      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      #  Deploy container to Web App (Managed Identity pulls image)
      - name: Deploy container to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: my-churn-api-shraddha
          images: ${{ secrets.ACR_SERVER }}/my-churn-api:latest
          slot-name: 'Production'
